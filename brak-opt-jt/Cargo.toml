[package]
name = "brak-opt-jt"
version = "0.1.0"
edition = "2021"

[dependencies]
brak-core = { path = "../brak-core" }
brak-ir-lir = { path = "../brak-ir-lir" }
brak-opt-traits = { path = "../brak-opt-traits" }
