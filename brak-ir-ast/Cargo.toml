[package]
name = "brak-ir-ast"
version = "0.1.0"
edition = "2021"
description = "Brak AST definition + serde + pretty printer"

[dependencies]
brak-core = { path = "../brak-core" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
